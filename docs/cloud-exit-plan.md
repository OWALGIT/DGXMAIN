# תוכנית יציאה מהענן — Cloud Exit Plan

נלווה ל־[`cloud-inventory.md`](./cloud-inventory.md). מבוסס על מדידות 2026-08-17.

---

## החסם האמיתי: ארכיטקטורה, לא כוח

```
הענן:      108 vCPU · 429 GB RAM · 1.64 TB data   — כולו x86_64
on-prem:    56 vCPU · 273 GB RAM · 5.7 TB פנוי
             ├─ dgxmain   20c/121GB  aarch64  ← לא מריץ x86 images
             ├─ dgxsec    20c/121GB  aarch64  ← לא מריץ x86 images
             └─ 5060ihome 16c/ 31GB  x86_64   ← זה כל ה-x86 שיש
```

דיסק לא בעיה (5.7 TB פנוי מול 1.64 TB בשימוש). RAM בהקצאה נראה חסר, אבל
בפועל השימוש נמוך בהרבה והכפילויות (סעיף 7 באינוונטורי) אוכלות את הרוב.
**הבעיה היא ש‑31 GB של x86 on-prem צריכים לספוג צי שכולו x86.**

### שימוש בפועל — נמדד, לא מוקצה

| מכונת ענן | RAM בשימוש | load (1m) | דאטה (docker volumes) |
|---|---|---|---|
| `storai` כולל 3 ה־VMs שלו | 105 GB (מזה ~85 GB RSS של ה־VMs) | 33.2 | qcow2: dioneto 80G |
| └ `openwebui-vps` (guest) | 26 GB | 10.1 | 3.8 GB |
| └ `vps-dioneto` (guest) | 9.2 GB | 0.5 | 16 GB |
| └ `nitay` | ~11 GB RSS | — | — |
| `nerve-hub` | 11.3 GB | 2.1 | 315 MB |
| `vpscld` | 5.8 GB | 6.6 | 8.4 GB |
| `arcai` | 5.8 GB | 0.4 | 27 GB (sefarim 12G, cyber 8.6G, cs 6.3G) |
| `vps-gra6` | 5.3 GB | 0.5 | 15 GB |
| `bit001` | 4.5 GB | 4.0 | 21 GB |
| `vps-office` | 4.2 GB | 6.4 | 1.4 GB |
| `aiapi` | 3.5 GB | 0.3 | 47 MB |
| `vps-custmer` | 1.9 GB | 0.07 | 49 MB |
| **סה"כ** | **~110–150 GB** (מול 429 GB מוקצים) | **~64** | **~67 GB volumes + ~110 GB VM images** |

on‑prem פנוי כרגע, תחת העומס הקיים: `dgxmain` 112.8 GB · `dgxsec` 113.5 GB ·
`5060ihome` 21.5 GB → **~248 GB RAM זמין**, ו‑~6.6 TB דיסק פנוי.

מכאן: **דיסק ו‑RAM מספיקים בנוחות. CPU צמוד** (עומס מצטבר ~64 מול ~53 ליבות פנויות —
עובד רק אחרי איחוד הכפילויות). **הארכיטקטורה היא החסם היחיד שלא נפתר בעצמו:**
מתוך 248 GB הזמינים, 226 GB הם aarch64 ורק 21.5 GB הם x86.

⚠️ ול‑DGX Spark יש שיקול נוסף: ה‑121 GB הם **זיכרון מאוחד** משותף ל‑CPU ול‑GPU.
כל GB שסטאק Docker לוקח שם הוא GB שנגרע ממודלים. זה הנימוק החזק לקנות מכונת x86
נפרדת במקום למלא את ה‑DGX בשירותים.

---

## מה באמת בשימוש — נבדק מול הדאטה עצמו

לא לפי עומס אלא לפי השאלה "מתי לאחרונה נכנס דאטה". נמדד 2026-08-17.

### חי — יש דאטה אמיתי ועדכני

| שירות | מכונה | הראיה |
|---|---|---|
| **Paperless** (מוצר DioNeto) | `vps-dioneto` | **10,121 מסמכים**, אחרון היום |
| **LiteLLM gateway** | `nerve-hub` | 456 קריאות, אחרונה **07:34 היום**. openai/MAIN 151, openai/auto 150, mistral |
| **LibreChat של OwalAI** | `vpscld` | 76 שיחות · 1,887 הודעות · אחרונה 15.08 |
| **MCP hub** | `bit001` | דרכו מנוהל כל הצי (כולל הסקר הזה) |
| Langflow | `nerve-hub` | 31 flows |
| Open WebUI | `openwebui-vps` | 36 שיחות, משתמש אחד |
| Nextcloud | `vpscld` / `vps-dioneto` | 1 ו‑2 משתמשים · 291 MB |

### זבל — אפס שימוש או נטוש

| מה | הראיה |
|---|---|
| **7 מתוך 8 סטאקי LibreChat בענן** | `aiapi` 0 שיחות · `vps-gra6` 0 · `vps-office` 0 · `nerve-hub` 0 · `arcai` 2 שיחות/4 הודעות · `vps-custmer` 1/2 · `storai` נטוש מאז **13.06**. ≈42 קונטיינרים |
| **2 סטאקי Dify מלאים** | `nerve-hub` ו‑`openwebui-vps`: **0 workflow runs** בשניהם, הודעה אחת כל אחד. ≈30 קונטיינרים |
| **המכונה `aiapi` כולה** | קונטיינר ה‑API: **0 שורות לוג ב‑24 שעות** · ה‑gateway עונה רק ל‑`GET /health` של עצמו (2,849 מתוכן) · `admin-panel` בלופ restart · n8n לא נכתב מאז 16.07 |
| **Wazuh SIEM** | 3 מתוך 5 agents **מנותקים** (dgxmain, dgxsec, gama-2) · ה‑dashboard לא עונה (HTTP 000) |
| Gitea על `nerve-hub` | **repo אחד**, בזמן ש‑`5060ihome` מריץ Gitea עם 220 GB דיסק ייעודי |
| Frigate על `arcai` | אין הקלטות בכלל |
| Dokploy + Kortix + Supabase + OpenHands + Whisper | `openwebui-vps` — up 6 שבועות, אין שום עדות לשימוש |
| n8n ×2 | `aiapi` נכתב לאחרונה 16.07 · `vps-dioneto` 31.07 |

### מה זה עושה לתוכנית

מה שבאמת צריך לעבור: Paperless, LibreChat אחד, LiteLLM, Langflow, Open WebUI,
27 GB ספריות מ‑arcai, WordPress אחד (`bitwp`, 82 טבלאות) וה‑MCP hub. **סדר גודל של
~100 GB דאטה ו‑5–6 שירותים** — לא 13 מכונות.

לכן: **5 מתוך 9 החיובים אפשר לבטל כמעט בלי הגירה:**

| מכונה | מה יש עליה באמת | מה צריך להעביר קודם |
|---|---|---|
| `vps-custmer` | שיחת בדיקה אחת | כלום |
| `aiapi` | שום דבר שמשרת תעבורה | לוודא שה‑`biton-ai-api` לא מחובר לכלום חיצוני |
| `vps-gra6` | Wazuh חצי‑שבור, registry, Nomad בודד | להחליט אם Wazuh בכלל נשאר |
| `arcai` | ספריות (sefarim 12G, cyber 8.6G, cs 6.3G) | 27 GB + לבדוק את המצלמות |
| `vps-office` | WordPress אחד אמיתי | `bitwp` |

ומכיוון שהיקף האמיתי כל כך קטן, **דרך ב' (בנייה מחדש כ‑arm64) נעשית סבירה** —
צריך להמיר 5–6 שירותים, לא צי שלם. Wazuh, שהוא החסם הגדול ב‑arm64, ממילא שבור.

### מה לא הצלחתי לאמת

GLPI (סיסמת DB), ה‑WordPress השני על `vps-office`, מספר ה‑workflows ב‑n8n (רק זמני קבצים),
והמכונה `84.247.130.13` (אין SSH). שאר שירותי DioNeto — FreeScout, UVdesk, Metabase,
Vaultwarden — לא נבדקו אחד‑אחד, אבל המכונה עצמה חיה בבירור.

---

## עלויות — מתוך החשבוניות עצמן

מקורות: **חשבונית OVH `WE4296746`** (01.05.2026) ו**חשבונית Contabo `151338090001`**
(31.07.2026), שתיהן ב‑Google Drive. הסכומים כאן מועתקים מהן, לא מוערכים.

### OVH — $391.79 לחודש

חשבון `meni@biton.pro`, מחויב דרך HEBERGEMENT OVH INC (קנדה), 0% VAT.

| שורת חשבונית | מכונה | בסיס | תוספות | **סה"כ/חודש** |
|---|---|---|---|---|
| `GAME-2` \| AMD Ryzen 9 9950X3D · `cloudai.yohay.ai` · ns3027097 = **162.19.126.209** | ⚠️ **לא נגישה בשום פורט** | $213.00 | +$29.90 (128GB DDR5) | **$242.90** |
| `KS-7` \| AMD Epyc 7451 · `stor00.yohay.ai` · ns3181246 = 51.195.88.44 | `storai` | $69.00 | +$35.40 (256GB DDR4 ECC) | **$104.40** |
| `KS-STOR` \| Intel Xeon-D 1521 · `arc.yohay.ai` · ns31244819 = 51.178.66.135 | `arcai` | $26.60 | +$17.70 (4×6TB HDD + 500GB NVMe) | **$44.30** |
| vRack ×3 · vRack Services ×2 · Veeam Backup Enterprise · KMS · DBaaS Logs | — | $0.00 | | $0.00 |
| KMS secret consumption (6 יחידות) | — | | | $0.19 |

**לא מופיעות בחשבונית הזו:** `vps-custmer` (152.228.142.1), `vps-gra6` (213.32.66.128),
`vps-office` (213.32.69.7), ופרויקט ה‑Public Cloud `owalai`
(`a17a6b5cc7ee4f01a9858baa32f34bbd`) — מחויבים בנפרד, בסכום שלא ראיתי.

### Contabo — $103.15 לחודש (steady state)

לקוח 15133809. החשבונית עצמה $110.71 כי היא כוללת שתי תקופות ל‑`vpscld` וחיובים חלקיים.

| שורת חשבונית | מכונה | **סה"כ/חודש** |
|---|---|---|
| Cloud VPS 60 NVMe · 45.88.191.3 + NVMe Storage Extension | `nerve-hub` | **$67.70** ($58.80 + $8.90) |
| Cloud VPS 20 NVMe · 194.146.12.114 + NVMe Storage Extension | `bit001` | **$12.95** ($9.00 + $3.95) |
| Cloud VPS 20 NVMe · 167.86.110.205 | `vpscld` | **$9.00** |
| Cloud VPS 20 NVMe · 31.187.74.8 | `aiapi` | **$9.00** |
| Additional IPs (1) · 84.247.130.13 | **לא שרת — IP נוסף** | **$4.50** |

תיקון למיפוי: `84.247.130.13` **אינו מכונה נפרדת** אלא IP נוסף על אחד ה‑VPS הקיימים.
זה מסביר למה אין לו SSH משלו.

### סה"כ ידוע: **$494.94 לחודש ≈ $5,939 לשנה**

ולא כולל 3 VPS‑ים ב‑OVH ואת פרויקט ה‑Public Cloud.

### השורה שמסבירה הכל

**`GAME-2` — $242.90 לחודש, 49% מחשבון OVH וכמחצית מכל ההוצאה הידועה — לא עונה לכלום.**
אין ICMP, פורטים 22/80/443/8006 סגורים, ה‑cloudflared tunnel שלו `gama2` **down**,
ה‑Wazuh agent `gama-2` **Disconnected**, והוא לא ב‑Tailscale. עדיין יש לו 12 רשומות DNS
פעילות (pbx, sip, k8s, servers, status, dev, customers, wpbit...).
**זה $2,915 בשנה.** לפני ביטול — לאמת בפאנל OVH שהוא באמת לא משמש (ייתכן חומת אש
שמגבילה ל‑Cloudflare בלבד, אבל אז גם ה‑tunnel וה‑agent לא היו נופלים).

### עלות מול שימוש בפועל

| מכונה | $/חודש | RAM בשימוש | ניצולת | מה חי עליה |
|---|---|---|---|---|
| **GAME-2** | **$242.90** | — | — | **כלום שאני מצליח לראות** |
| `storai` | $104.40 | 105 GB / 251 | 42% | רק ה‑VM של DioNeto; שירותיה עצמה נטושים מיוני |
| `nerve-hub` | $67.70 | 11.3 GB / 94 | **12%** | LiteLLM + Langflow. Dify 0 runs, Gitea repo אחד, LibreChat ריק |
| `arcai` | $44.30 | 5.8 GB / 15 | 39% | 27 GB ספריות. Frigate בלי הקלטות |
| `bit001` | $12.95 | 4.5 GB / 11 | 38% | **ה‑MCP hub — חי** |
| `vpscld` | $9.00 | 5.8 GB / 11 | 53% | **LibreChat של OwalAI — חי** |
| `aiapi` | $9.00 | 3.5 GB / 11 | 29% | **שום דבר** |
| IP נוסף | $4.50 | — | — | `df001` |
| `vps-custmer` · `vps-gra6` · `vps-office` | לא ידוע | 1.9 · 5.3 · 4.2 GB | 50/24/36% | כלום · Wazuh שבור · WordPress אחד |

`storai` + `nerve-hub` = **$172.10 לחודש**, וכל מה שחי עליהן הוא Paperless, gateway
של מודלים ו‑31 flows. שלושתם נכנסים למכונה אחת או ישר ל‑DGX.

### סדר ביטול לפי חיסכון

| # | מה | חיסכון/חודש | מה צריך להעביר קודם |
|---|---|---|---|
| 1 | **GAME-2** (162.19.126.209) | **$242.90** | כלום — רק לאמת, ולמחוק 12 רשומות DNS |
| 2 | `storai` (KS-7) | $104.40 | VM אחד: `vps-dioneto` כ‑qcow2 |
| 3 | `nerve-hub` | $67.70 | LiteLLM + Langflow |
| 4 | `arcai` (KS-STOR) | $44.30 | 27 GB ספריות + בדיקת מצלמות |
| 5 | `aiapi` | $9.00 | כלום |
| 6 | IP נוסף 84.247.130.13 | $4.50 | להכריע מה עם `df001` |
| | **סה"כ** | **$472.80/חודש ≈ $5,674/שנה** | |

נשאר לשלם עד שה‑hub עובר הביתה: `vpscld` $9 + `bit001` $12.95 = **$21.95/חודש**.

זה גם משנה את "שלב 6" למטה: `storai` מסומן שם אחרון בגלל תלות ה‑VMs, אבל התלות היא
VM אחד חי. כספית, `GAME-2` צריך להיות ראשון והוא לא דורש הגירה בכלל.

לפני ביטול `storai` צריך להכריע שני דברים: מה עושים עם `openwebui-vps` (36 שיחות
Open WebUI, ומריץ את ה‑cloudflared של `vpsall`), ומה זה ה‑VM `nitay`.

### שתי הערות תפעוליות

1. **חשבון OVH הוא של `meni@biton.pro`**, לא שלך. לביטול שלוש המכונות היקרות
   צריך גישה לחשבון שלו.
2. **מפתחות OVH API שוכבים כטקסט גלוי ב‑Google Drive** (`OVH.txt`, `OVH4.txt`,
   7 עותקים) — application key, secret ו‑consumer key. ניסיתי אותם והם מחזירים
   403, כלומר כבר מבוטלים, אבל צריך למחוק אותם משם בכל מקרה.

### מה עוד אין לו מספר

- 3 ה‑VPS ב‑OVH ופרויקט `owalai` — צריך את החשבונית השנייה של OVH.
- `cloudmgmt` (על `vps-office` ו‑`vps-gra6`) בנויה בדיוק בשביל זה — יש בה טבלת
  `billing_snapshots` עם `provider/amount/currency/period` — אבל היא **ריקה,
  0 שורות**, וגם `cloud_credentials` ריקה. האפליקציה הוקמה ולא חוברה לספקים.
- GCP: `list_billing_accounts` מחזיר רשימה ריקה.
- LiteLLM: 456 קריאות עם **spend 0.0000 בכולן** — או endpoints חינמיים או שתמחור
  לא הוגדר. אין שם עלות למדוד.

---

שלוש דרכים, צריך להחליט לפני שמתחילים להעביר:

| דרך | מה עושים | עלות | סיכון |
|---|---|---|---|
| **א. לקנות מכונת x86 אחת** (מומלץ) | שרת 128–256 GB RAM + 2×NVMe בבית/משרד. הצי עובר כמעט as-is | חד-פעמי | נמוך — `docker compose up` ותו לא |
| **ב. לבנות מחדש כ‑arm64** | לבנות מחדש את הסטאקים על ה-DGX | 0 ₪, הרבה עבודה | בינוני–גבוה. ל-LibreChat/Dify/n8n/Nextcloud/Postgres/Mongo יש arm64 (מוכח: dify-api רץ arm64 על dgxmain). ל-Wazuh, ולחלק מ-image ה-MCP המקומיים — אין |
| **ג. היברידי** | להשאיר מכונת ענן אחת קטנה ל-x86 שלא מומר | חיוב אחד ממשיך | נמוך, אבל לא "ניתוק" |

---

## סדר הפעולות

הכלל שמכתיב את הסדר: **אל תכבה את מה שדרכו אתה מנהל.** ה-MCP hub הוא `bit001` בענן.

### שלב 0 — קיבוע (יום)
- להריץ `./bin/fleet` ולתקן את `inventory/fleet.hosts` (השינויים כבר ב-branch הזה).
- להשיג גישת SSH ל-3 העיוורים: `c001`, `meni-office0-0001`, `84.247.130.13`.
  בלי זה, המיפוי לא סגור ואי אפשר לבטל חיוב Contabo בביטחון.
- snapshot של כל מכונת ענן לפני שנוגעים.

### שלב 1 — להחזיר את השליטה הביתה (שבוע)
1. להקים MCP hub שני על `dgxmain` (או על מכונת ה-x86 החדשה) במקביל ל-`bit001`.
2. להעביר את LiteLLM מ-`nerve-hub` ל-on-prem; להפנות `llm-mcp.yohay.ai` פנימה.
3. לאמת שהצי כולו מנוהל מה-hub החדש **לפני** שמכבים את `bit001`.

### שלב 2 — דאטה לפני אפליקציות (1–2 שבועות)
| מה | מאיפה | גודל |
|---|---|---|
| `claude-memory` (NFS rw) | storai → מקומי על dgxsec | תלוי בשימוש |
| `sefarim` (NFS ro) | arcai `/store/backup` | 134 GB |
| נתוני storai | storai | 962 GB |
| Storj buckets | Storj → דיסק מקומי | לפי הצורך |
| Nextcloud ×3, Paperless, GLPI, FreeScout, Vaultwarden, Metabase | vpscld + dioneto | כלול ב-68G/73G |

לאחר ההעתקה: להוריד את ה-NFS mounts מ-`dgxsec` ולהצביע למקומי. זה מנתק את התלות
הכי מסוכנת שיש — on-prem שתלוי ב-OVH.

### שלב 3 — לאחד, לא להעביר (1–2 שבועות)
כאן ההרווח האמיתי. **לא להעביר 13 מופעי LibreChat** — להקים אחד (או שניים: prod+dev),
לייבא את ה-Mongo/Meilisearch של המופעים שבאמת בשימוש, ולמחוק את השאר.
אותו דבר ל-WordPress (5→1 עם multisite), Dify (3→1), n8n (4→1), Open WebUI (4→1),
Gitea (2→1, זה שעל `5060ihome` עם 220G דיסק ייעודי).

### שלב 4 — סטייטפול ומוצרים (2–3 שבועות)
- מוצר **DioNeto** (`vps-dioneto`) הוא הסטאק המסובך — 40 קונטיינרים עם SSO, DB-ים ותורים.
  להעביר אותו כ-VM שלם (`virsh dumpxml` + העתקת qcow2) ולא קונטיינר-קונטיינר.
- **Wazuh** על `vps-gra6` — אין arm64. או למכונת ה-x86 החדשה, או לוותר.
- **Frigate** על `arcai` — לבדוק לאן המצלמות מצביעות לפני ההזזה.

### שלב 5 — חשיפה ציבורית (שבוע)
מאחורי NAT של Cellcom, בלי IP סטטי, אין ingress בלי מתווך. שתי אפשרויות:
- **להשאיר Cloudflare Tunnels** — עובד היום (8 tunnels healthy), לא עולה, אבל זה עדיין ענן.
- **IP סטטי + reverse proxy משלך** — ניתוק אמיתי, דורש שינוי מול ה-ISP ופתיחת 80/443.

`5060ihome` כבר מריץ cloudflared פעיל — יש דפוס עובד להעתיק.

### שלב 6 — ביטול (יום)
לבטל רק אחרי 30 יום של הרצה מקבילה מוצלחת:

| ספק | מה לבטל | הערה |
|---|---|---|
| OVH | `162.19.126.209` | כבר מת — לבטל מיד, וגם למחוק 12 רשומות DNS מיותרות |
| OVH | `vps-custmer` (2c/3G, רק LibreChat) | הכי קל, אפשר ראשון |
| OVH | `vps-office`, `vps-gra6` | אחרי Wazuh ו-WordPress |
| OVH | `arcai` (bare metal) | אחרי Frigate + sefarim |
| OVH | `storai` (bare metal) | **אחרון** — 4 VMs תלויות בו |
| Contabo | `aiapi`, `vpscld`, `nerve-hub`, `84.247.130.13` | אחרי LiteLLM+Nextcloud |
| Contabo | `bit001` | **ממש אחרון** — זו יד השליטה |

---

## מה לא לעשות

- לא לכבות `storai` לפני `openwebui-vps`/`vps-dioneto`/`nitay` — הן VMs עליו, ייעלמו איתו.
- לא לכבות `nerve-hub` לפני שהעברת LiteLLM — כל קריאות המודלים נופלות.
- לא להסתמך על מצב ה-tunnel כסימן חיים: `vpsall-*` מסומנים healthy למרות ש-`vpsall` כבוי
  (ה-cloudflared שלו רץ על openwebui-vps).
- לא למחוק את `/mnt/NASAI` על `5060ihome` — הוא **100% מלא**, כל כתיבה שם נכשלת כבר עכשיו.
  לטפל בזה לפני שמתכננים אותו כיעד migration.

## החלטה שצריכה להתקבל עכשיו

**דרך א, ב או ג?** כל השאר נגזר מזה. אם א — איזה תקציב חומרה, ואיפה המכונה תשב
(בית/משרד, וכמה upload יש שם). אם ב — צריך קודם לעבור image-image ולסמן מה אין לו arm64.
